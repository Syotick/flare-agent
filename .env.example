# Flare Agent 本地开发环境配置（复制为 .env 后按需修改；.env 不入库）
FLARE_ENV=dev
FLARE_LOG_LEVEL=INFO
# CORS（dev 默认放开；生产用 JSON 数组，如 FLARE_CORS_ORIGINS='["https://app.example.com"]'）
# FLARE_CORS_ORIGINS=["*"]

# PostgreSQL（docker-compose 默认账号）
FLARE_DATABASE_URL=postgresql://flare:flare@localhost:5432/flare
# Redis
FLARE_REDIS_URL=redis://localhost:6379/0

# 对象存储（本地用 MinIO 模拟 OSS）
FLARE_OBJECT_STORE_ENDPOINT=http://localhost:9000
FLARE_OBJECT_STORE_ACCESS_KEY=minioadmin
FLARE_OBJECT_STORE_SECRET_KEY=minioadmin

# 模型（默认 mock 供应商；接入真实模型时填 API Key）
FLARE_MODEL_PROVIDER=mock
FLARE_MODEL_API_KEY=
FLARE_MODEL_BASE_URL=http://localhost:9001/v1
# 控制台「模型」页本地持久化位置（真实环境变量优先于该文件；生产用 env/K8s Secret）
# FLARE_MODEL_CONFIG_PATH=data/model_config.json

# MCP 服务器列表（FR-2/FR-3，JSON；空=不连接任何服务器，无行为变化）
# FLARE_MCP_SERVERS=[{"name":"echo","url":"http://localhost:9001/mcp","transport":"streamable_http","headers":{},"enabled":true}]
# Skills 技能库目录（SKILL.md 声明式技能包）
FLARE_SKILLS_DIR=data/skills

# F9.3: OpenAI 兼容 API 认证（空=开放不校验；生产务必配置，如 FLARE_API_KEY=sk-xxx）
# FLARE_API_KEY=
# CLI 默认连接地址（F9.2，可选 --url 覆盖）
# FLARE_URL=http://127.0.0.1:8000

# F1.3/F2.4: 审批策略——等于或高于该权限级别的工具需人工审批（read|write|destructive）
# 默认 destructive：只有破坏性工具（如 sandbox_run）要审批；改 write 则写/破坏性都要
# FLARE_APPROVAL_REQUIRE_LEVEL=destructive
# 审批等待超时（秒），超时自动按拒绝处理
# FLARE_APPROVAL_TIMEOUT=300
# 审批后端：local（进程内，默认/单实例）| redis（多实例共享，跨节点决策轮询唤醒）
# FLARE_APPROVAL_BACKEND=local
# TOFU（首用信任）：同作用域某工具获批一次后后续自动放行，防审批疲劳
# FLARE_APPROVAL_TOFU=true
# TOFU 信任作用域：thread（会话线程，默认）| tenant | off
# FLARE_APPROVAL_TOFU_SCOPE=thread
