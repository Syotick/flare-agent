apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: flare-agent
  namespace: flare-agent
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: flare-agent
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
